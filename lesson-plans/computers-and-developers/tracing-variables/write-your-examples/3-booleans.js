'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare and initialize the variable 'isSunny' with its value
let isSunny = true;

// check the value of 'itsSunny' variable using an if-else statement
if (isSunny) {
  // If 'isSunny' is true, log a message to use sunscreen
  console.log('use your sunscreen');
} else {
  // If 'isSunny' is false, log a message indicating not to use sunscreen
  console.log('you do not your sunscreen');
}
