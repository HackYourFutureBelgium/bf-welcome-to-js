'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// declare a variable 'correct' and initialize it with the value true
let correct = true;

// read the value of the variable 'correct'
console.log(correct);

// reassign the variable 'correct' to the value false
correct = false;

// read the updated value of the variable 'correct'
console.log(correct);

// declare a variable 'getPermission' without initializing it
let getPermission;

// read the value of the variable 'getPermission' (which is currently undefined)
console.log(getPermission);

// assign the variable 'getPermission' the value true
getPermission = true;

// read the updated value of the variable 'getPermission'
console.log(getPermission);

