'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Define boolean variables
const isSunny = true;
const isRaining = false;

// Check weather conditions
if (isSunny) {
  console.log('It is sunny outside!');
} else if (isRaining) {
  console.log('It is raining outside!');
} else {
  console.log('The weather is unclear.');
}
