'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare and initialize the variable myFunction with its value = null
let myFunction = null;

if (myFunction === null) {
  console.log('The function is null.');
} else {
  myFunction(); // Attempting to call a null function would result in an error.
}
