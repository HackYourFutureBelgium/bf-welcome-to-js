'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Define null variables
const firstName = null;
const lastName = null;

// Check data conditions
if (firstName === null && lastName === null) {
  console.log('We missed soma data');
} else {
  console.log('Thank you for the full information');
}
