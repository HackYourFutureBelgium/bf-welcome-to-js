'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

/* declaring variables and initializing them to null */
let month = null;
let year = null;

/* reading the variables type */
console.log(typeof month);
console.log(typeof year);

/* reassigning values to the variables */
month = 'march';
year = 1995;

/* reading the values of the variables */
console.log('You were born in ' + month + ', ' + year);