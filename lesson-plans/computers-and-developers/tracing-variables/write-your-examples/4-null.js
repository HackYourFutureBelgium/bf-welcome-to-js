'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable initialized with null
let item = null;

// check if the item is null
if (item === null) {
  console.log('The item is null');
} else {
  console.log('The item is not null');
}
