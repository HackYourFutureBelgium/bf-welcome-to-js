'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// declare a variable 'theNull' and initialize it with the value null
let theNull = null;

// read variable 'theNull' with the value null
console.log(theNull);

// reassign the variable 'theNull' to a different value (in this case, null remains unchanged)
theNull = null;

// read the updated value of the variable 'theNull' which is null
console.log(theNull);

// declare a variable 'emptyValue' and initialize it with the value null
let emptyValue = null;

// read the value of the variable 'emptyValue' which is null
console.log(emptyValue);