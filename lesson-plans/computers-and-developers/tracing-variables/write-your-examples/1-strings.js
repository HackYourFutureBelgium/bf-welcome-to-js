'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare variable firstName
// Initialize value `jon`
let firstName = 'jon';

// Declare variable lastName
// Initialize value `deo`
let lastName = 'deo';

// Read the value stored into firstName and lastName
// Concatenate between space
// Declare variable fullName
// Assign value `jon deo`
let fullName = firstName + ' ' + lastName;

// Read the value stored in `fullName`
console.log(fullName);
