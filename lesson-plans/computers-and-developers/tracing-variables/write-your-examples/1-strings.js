'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

//declare a variable "Hello" and initialize it to the string "Hello"
let greeting = 'Hello';

//read and log the value of the greeting"Hello";
console.log(greeting);

// Reassigning the variable
greeting = 'Hi';

// Reading the variable again
console.log(greeting);
