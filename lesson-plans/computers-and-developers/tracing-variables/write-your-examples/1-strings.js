'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// declare the variable firstName and initialize it to the value 'rahwa'
let firstName = 'rahwa';
// declaire the variable lastName and initialize it to the value of 'kinfe'
let lastName = 'kinfe';
// read the variable firstName
// read the variable lastName
// declare the variable fullName initialize it to the value 'rahwa + kinfe'
let fullName = firstName + lastName;
// read the variable 'rahi'
// reassign the variable name and initialize it to the value of 'rahi'
firstName = 'rahi';
// read the variable fullName
console.log(fullName);
