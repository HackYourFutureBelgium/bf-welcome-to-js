'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare and initialize the variable 'country'

let country = 'Belgium';

// Declare the variable 'greeting', and read 'country' variable
let greeting = 'hi I am from' + country;

// Log the greeting into console
console.log(greeting);
