'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable star and initialize it to the value 'sun'
const star = 'sun'

//read the variable star with value 'sun'
console.log(star)

// declare a variable light
let light;

//assign a variable light the value 'sun'
light = 'sun';

// declare a variable blue and initialize it to the value 'sky'
let blue = 'sky';

//read the variable blue with value 'sky'
console.log(blue);

// assign the variable blue to the value 'water'
blue = 'water'

// declare a variable object and initialize it
let object = blue + ' and ' + light

//read the variable object
console.log(object)

//read the variable blue with value 'water'
//read the variable message
let message = `
the object
is blue:  ${blue}
`;

console.log(message)