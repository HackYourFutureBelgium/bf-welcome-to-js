'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// initialized variables value
const hello = 'Hello';
const world = 'world';
const awesome = "you're awesome";

// concatenate a new variable with already existing
const greeting = hello + ' ' + world + ', ' + awesome;

console.log(greeting); /* Hello world, you're awesome */
