'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable name and initialise to it value 'John Paul '
let name = 'John Paul '; 
 
// declare a variable city and initialise to it value 'Antwerp'
let city = 'Antwerp';  

// declare a variable name and initialise to it value name name + 'from ' + city 
let adress = name + 'from ' + city ; 
// read variable adress
console.log(adress); 

// declare variable message  and initialize it
let message = `
Hello my name is ${name} and I am from ${city}`;
// read variable message
console.log(message); 