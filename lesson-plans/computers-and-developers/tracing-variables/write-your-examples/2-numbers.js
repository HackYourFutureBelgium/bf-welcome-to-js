'use strict';

/* Numbers

TODO: create a few variables(numbers):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Declare and initialize the variable age and its value
let age = 25;

// Declare the variable 'nextYear' and initializing it by adding 1 to the 'age' variable
let nextYear = age + 1;

// log the variable nextYear into console
console.log(nextYear);
