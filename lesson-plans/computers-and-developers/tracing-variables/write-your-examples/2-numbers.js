'use strict';

/* Numbers

TODO: create a few variables(numbers):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// initialized variables value
const date = 5;
const monthNumber = 5;
const year = 1990;

// concatenate a new variable value with already existing variables
const myBirthDay = 'My birthday is ' + date + '/' + monthNumber + '/' + year;

console.log(myBirthDay); /* My birthday is 5/5/1990 */
