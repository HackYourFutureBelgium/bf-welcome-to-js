'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// Variable Declaration
let undefinedVariable;

// Variable Initialization
// The variable is declared but not initialized, so its value is undefined

// Reading the Variable
console.log(undefinedVariable);

// Reassigning the Variable
undefinedVariable = 42;

// Reading the Reassigned Variable
console.log(undefinedVariable);
