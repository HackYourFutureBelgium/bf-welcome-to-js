'use strict';

/* Undefined

- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// declare a variable 'undefinedVariable' initializing it to value 'undefined
let undefinedVariable;

// read the value of the variable 'undefinedVariable' (which is currently undefined)
console.log(undefinedVariable);

// assign a value of undefined to the variable 'undefinedVariable'
undefinedVariable = undefined;

// read the updated value of the variable 'undefinedVariable'
console.log(undefinedVariable);

// declare a variable 'anotherUndefined' and initialize it with the value undefined
let anotherUndefined;

// read the value of the variable 'anotherUndefined' (which is currently undefined)
console.log(anotherUndefined);
