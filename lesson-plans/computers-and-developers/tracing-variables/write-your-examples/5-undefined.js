'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
// This code attempts to catenate a string with an undefined variable
let name;

// This will result in 'Hello,undefined'because 'name' is undefined
console.log('Hello' + name);
