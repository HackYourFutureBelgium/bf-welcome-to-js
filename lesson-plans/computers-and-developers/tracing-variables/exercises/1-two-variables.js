'use strict';

/* Two Variables

  Can you predict what the [trace] button will log to the console?
    use the [table] button to write down your predictions

  study tips: [variables], [open in] jsTutor, [ask me]

*/

// trace the program a few times with different values

/*declared the variable x and asigned value Pieter to it*/
let x = 'Pieter';

/*skipped lines 18 and 19, because they ate commented*/
// let x = 'Blake';
// let x = 'George';

/*declared variable y and asigned value hello concatenated with variable x*/

let y = 'hello ' + x;

/*red the variavle y, output is hello Pieter*/
console.log(y);

/*variable y is reasigned with new value good bye concatanated with variable x*/
y = 'good bye ' + x;

console.log(y);
/*red the variable y, the output is good bye Pieter*/
