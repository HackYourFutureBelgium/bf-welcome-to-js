'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// Declare variable name 'first'
// Initialize the value 'first' to "Pieter"
let first = 'Pieter';

// Declare variable name 'last'
// Initialize the value 'last' to 'Rabbit'
let last = 'Rabbit';

// Declare variable name 'fullName'
// Initialize the value 'fullName' to 'first' + ' ' +'last'
let fullName = first + ' ' + last;

// Read the value stored to 'fullName"
console.log(fullName);

// Declare variable name 'greeting' ;
// Initialize the value 'greeting' to 'hello ' + fullName;
let greeting = 'hello ' + fullName;

// READ the value stored in `greeting`
// CONCATENATE "good bye" and "fullName"
// ASSIGN "good bye Pieter Rabbit" to `greeting`
greeting = 'good bye ' + fullName;

// Read the value  name 'greeting';
console.log(greeting);
