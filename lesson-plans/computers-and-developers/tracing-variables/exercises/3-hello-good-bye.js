'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/
// Declare and initialize variables for first and last name
let first = 'Pieter';
let last = 'Rabbit';

// Combine first and last name to form the full name
let fullName = first + ' ' + last;

// Log the full name to the console
console.log(fullName);

// Create a greeting by concatenating 'hello' with the full name
let greeting = 'hello ' + fullName;

// Reassign greeting to 'good bye' followed by the full name
greeting = 'good bye ' + fullName;

// Log the updated greeting to the console
console.log(greeting);
