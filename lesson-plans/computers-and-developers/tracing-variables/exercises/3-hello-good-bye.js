'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// Declare a variable named 'first'
// Initialize the value to "Pieter"
let first = 'Pieter';

// Declare a variable named 'last'
// Initialize the value to "Rabbit"
let last = 'Rabbit';

// concat first and last name with space in between
let fullName = first + ' ' + last;

// log the variable fullname
console.log(fullName);

/*
 declare a variable named 'greeting'and
 initialize its value to hello string concatenated with fullName
 */
let greeting = 'hello ' + fullName;

// assign greeting with good bye concatenated with fullName
greeting = 'good bye ' + fullName;

// log the variable greeting
console.log(greeting);
