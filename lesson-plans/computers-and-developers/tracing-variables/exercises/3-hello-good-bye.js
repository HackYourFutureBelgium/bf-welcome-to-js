'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// Define the first name
let first = 'Pieter';

// Define the last name
let last = 'Rabbit';

// Combine the first and last names to create the full name
let fullName = first + ' ' + last;

console.log(fullName);

// Create a greeting message using the full name
let greeting = 'hello ' + fullName;

// Overwrite the greeting with a farewell message
greeting = 'good bye ' + fullName;

// Log the final greeting message to the console
console.log(greeting);
