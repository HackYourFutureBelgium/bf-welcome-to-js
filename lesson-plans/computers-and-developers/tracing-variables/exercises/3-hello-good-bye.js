'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// Assigned value to the variable
let first = 'Peter';
let last = 'Rabbit';

// Concatenated the value of two variables in one variable
let fullName = first + ' ' + last;

console.log(fullName);

// Concatenated a new value with already existing variable
let greeting = 'hello ' + fullName;

// Reassigned "greeting" to the new value
greeting = 'good bye ' + fullName;

console.log(greeting);
