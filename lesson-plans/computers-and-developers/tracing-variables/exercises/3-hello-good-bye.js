'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// declaration of the first and last name
let first = 'Pieter';

console.log (first);

let last = 'Rabbit';

console.log(last);

// concatenation of 2 variables leaving a blank space between them //
let fullName = first + ' ' + last;

// printing the concatenated variable
console.log(fullName);

// declaring the variable "greeting" and ssigning it a value and concatenating with the privious variable
let greeting = 'hello ' + fullName;

// reassinging the values to the "greeting" variable
greeting = 'good bye ' + fullName;

// printinf the whole message in the concatenated variable 
console.log(greeting);
