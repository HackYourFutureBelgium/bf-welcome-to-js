'use strict';

/* Comment & Log

  Add comments and logs to help a developer understand this program

*/

// Declare variable `first`
// Initialize value `Pieter`
let first = 'Pieter';

// Declare variable `last`
// Initialize value `Rabbit`
let last = 'Rabbit';

// Read the value of variable `first` and last
// Concatenate with space
// Declare variable `fullNAme`
// Assign value to `Pieter Robbit`
let fullName = first + ' ' + last;

//Read the value stored in `fullName`
console.log(fullName);

// Read the the value stored in `fullName`
// Concatenate with `hello `
// Declare variable `greeting`
// Assign value `hello Pieter Rabbit`
let greeting = 'hello ' + fullName;

// Read the value stored in to `fullName`
// Concatenate with `good bye `
// Reassign value `good bye Pieter Rabbit` to `greeting`
greeting = 'good bye ' + fullName;

// Read the value stored in `greeting`
console.log(greeting);
