'use strict';

debugger;

/* Numbers

  1- numbers are a data type used to work with numbers

  2-JavaScript has only one type of number.

  3- Numbers can be written with or without decimals.

*/

// declare a variable zero and initialize it with the value 0
let zero = 0;
let two = 2;

// log the variable zero
console.log(zero);
console.log(two);

// whole number or integer
let population = 11509000;
let pib = 265187300;

// log the variable wholeNumber
console.log(population);
console.log(pib);

// decimal number or float
let gdp = 594.1;
let roGdp = 284.1; // Billion dollars in 2021

// log the variable gdp
console.log(gdp);
console.log(roGdp);

let message = `
in 2021,
Belgium population :  ${population}
and it's GDB : ${gdp}
`;
let messages = `
In 2023,
Romanian population : ${pib}
and it's GBD : ${roGdp}
`;

console.log(message);
console.log(messages);
