// #todo

'use strict';

// first <- 'angus'
const first = 'angus';

// last <- 'croll'
const last = 'croll';

// greeting <- 'hello ' + first + ' ' + last
let greeting = 'hello ' + first + ' ' + last;

// greeting <- greeting + '!'
greeting = greeting + '!';

console.log(greeting);
