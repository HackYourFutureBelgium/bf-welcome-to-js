// #todo

'use strict';

// first <- 'angus'
let first = 'angus';

// last <- 'croll'
let last = 'croll';

// greeting <- 'hello ' + first + ' ' + last
let greeting = 'hello ' + first + ' ' + last;

// greeting <- greeting + '!'
greeting = greeting + '!'

console.log(greeting)
