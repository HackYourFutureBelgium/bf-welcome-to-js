// #todo

'use strict';

let first = 'angus';
let last = 'croll';

let greeting = 'hello ' + first + ' ' + last;

greeting = greeting + '!';

console.log(greeting); // 'hello angus croll!'

/*
  - mark each variable use with a different color (like in the ?variables lens)
  - use lines to connect each variable declaration to it's uses

  done with these questions?  try "ask me" and "random line"

  On line 5 there is a a string:
- How is this data used? -- The data used as a string and the variable is 'first'
- What purpose does it have in the program? -- call the first name
- Is it important later on in the program? -- yes
*/
